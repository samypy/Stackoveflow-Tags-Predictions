import pandas as pd

# Read the Excel file
excel_file = 'path/to/your/excel/file.xlsx'
excel_data = pd.read_excel(excel_file, sheet_name=None)

# Combine NAM sheets
nam_df = pd.concat([excel_data['NAM 1'], excel_data['NAM 2']])

# Combine EMEA sheets
emea_df = pd.concat([excel_data['EMEA 1'], excel_data['EMEA 2']])

# Combine APAC sheets
apac_df = pd.concat([excel_data['APAC 1'], excel_data['APAC 2']])

# Access the Global sheet
global_df = excel_data['Global']

# Perform further operations or analysis on the combined dataframes

import pandas as pd

# Assuming your dataframe is named 'df'

# Define the months to be removed
months_to_remove = ['Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec']

# Create a boolean condition to filter rows
condition = (df['year'] != 2023) | (~df['month'].isin(months_to_remove))

# Apply the condition to the dataframe
filtered_df = df[condition]

# Display the resulting dataframe
print(filtered_df)


import pandas as pd

# Assuming your dataframe is named 'df'

# Create the pivot table
pivot_table = pd.pivot_table(df, values='Duration', index=['Year', 'Month'], columns='Product', aggfunc='sum')

# Display the pivot table
print(pivot_table)
